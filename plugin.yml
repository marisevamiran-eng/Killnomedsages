name: KillMessage
version: 1.0.0
main: com.example.killmessage.KillMessage
api-version: '1.21'
author: YourName
description: Kill messages for boxpvp

commands:
  killmessage:
    description: Reload config
    usage: /killmessage reload
    permission: killmessage.reload
